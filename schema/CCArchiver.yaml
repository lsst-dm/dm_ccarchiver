$schema: http://json-schema.org/draft-07/schema#
$id: https://github.com/lsst-dm/dm_CCArchiver/blob/master/schema/CCArchiver.yaml
# title must end with one or more spaces followed by the schema version, which must begin with "v"
title: CCArchiver v1
description: Schema for CCArchiver configuration files
type: object
forwarder:
    forwarder_consume_queue: f98_consume
    forwarder_publish_queue: cc_foreman_ack_publish
